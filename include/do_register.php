<?php

$checkFirst = "";
$checkLast = "";
$checkEmail = "";

  /*foreach ($_POST as $key => $value) {

	  $temp = is_array($value) ? $value : trim($value);
  	  // validation
      if(!filter_var($value === $_POST['firstname'], FILTER_SANITIZE_STRING, FILTER_FLAG_NO_ENCODE_QUOTES))
      {
          $checkFirst = "$firstname is not the real name!";
      }
      if(!filter_var($value === $_POST['lastname'], FILTER_SANITIZE_STRING, FILTER_FLAG_NO_ENCODE_QUOTES))
      {
          $checkLast = "$lastname is not the real name!";
      }

      if (filter_var($value === $_POST['email'], FILTER_VALIDATE_EMAIL === false)) {
        $checkEmail = "$email is not a valid email address";
      }

  	  // create varibles with values from post array
  	  ${$key} = $temp;
    


	
}*/
?>